

<script>
  plugin_path = '../assets/';
</script>

<!--Interventions Form-->


<div class="container">

			<header class="text-center mb-60 mt-40">
				<h2>New Intervention Request</h2>
				<hr />
			</header>

			<div class="row">
				<!-- FORM -->
					<div class="container col-5 mb-5 text-center">
                     <form action="" method="" enctype="multipart/form-data" id="intForm">
                       <label for="clients">Select Client</label>
					   <%= collection_select(:customer, :customer_id, Customer.all, :id, :company_name, prompt: true) %>
                       <br/>
                       <br/>
                       <label for="client-buildings">Select Building</label>
					   <%= collection_select(:building, :customer_id, Building.all, :customer_id, :administrator_full_name, prompt: true) %>
                       <br/>
                       <br/>
                       <label for="buildings-batteries">Select Battery</label>
					   <%= collection_select(:battery, :building_id, Battery.all, :id, :id, prompt: true) %>
                       <br/>
                       <br/>
                       <label for="batteries-columns">Select Column</label>
					   <%= collection_select(:column, :battery_id, Column.all, :id, :id, prompt: true) %>
                       <br/>
                       <br/>
                       <label for="columns-lift">Select Lift</label>
					   <%= collection_select(:elevator, :column_id, Elevator.all, :id, :id, prompt: true) %>
                       <br/>
                       <br/>
                       <label for="columns-lift">Company's employees</label>
					   <%= collection_select(:employe, :column_id, Employee.all, :id, :first_name, prompt: true) %>
                       <br/>
                       <br/>
                       <label for="description">Intervention Description</label>
                       <input required type="text" value="" class="form-control" name="description" id="descrFrm">
                       <button type="submit" class="btn btn-rblue" id="btnForm" style="background-color:#EB2C55;"><i class="fa fa-check"></i>SUBMIT</button>
                    </div>
                      
                    </div>

            </div>

</div><!--End main form coantainer-->

<a href="#" id="toTop">
  <span class="arrow-top">▲</span>
</a>

