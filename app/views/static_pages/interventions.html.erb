

<script>
  plugin_path = '../assets/';
</script>

<!--Interventions Form-->


<div class="container">

			<header class="text-center mb-60 mt-40">
				<h2>New Intervention Request</h2>
				<hr />
			</header>

			<div class="row">
				<!-- FORM -->
					<div class="container col-5 mb-5 text-center">
                     <form action="/interventions" method="POST" enctype="multipart/form-data" id="intForm">
                       <label for="customer_id">Select Client</label>
					   <%= collection_select(:customer, :customer_id, Customer.all, :id, :company_name, {prompt: true}, {:id=>'select_customer', :name => 'customer_id'})  %>
                       <br/>
                       <br/>
                       <label for="building_id" class="d-none" id="build_label">Select Building</label>
					   <%= collection_select(:building, :customer_id, Building.all, :id, :id, {prompt: true}, {:id=>'select_build', :name => 'building_id', :class => "d-none"}) %>
                       <br/>
                       <br/>
                       <label for="battery_id" class="d-none" id="bat_label">Select Battery</label>
					   <%= collection_select(:battery, :building_id, Battery.all, :id, :id, {prompt: true}, {:id=>'select_bat', :name => 'battery_id', :class => "d-none"}) %>
                       <br/>
                       <br/>
                       <label for="column_id" class="d-none" id="col_label">Select Column</label>
					   <%= collection_select(:column, :battery_id, Column.all, :id, :id, {:prompt => "None"}, {:id=>'select_col', :name => 'column_id',:class => "d-none"}) %>
                       <br/>
                       <br/>
                       <label for="elevator_id" class="d-none" id="elev_label">Select Lift</label>
					   <%= collection_select(:elevator, :column_id, Elevator.all, :id, :id, {:prompt => "None"},{:id=>'select-elev', :name => 'elevator_id', :class => "d-none"}) %>
                       <br/>
                       <br/>
                       <label for="employee_id">Company's employees</label>
					   <%= collection_select(:employe, :employee_id, Employee.all, :id, :first_name, {:prompt => "None"},{:id=>'seelct_empl', :name => 'employee_id'}) %>
                       <br/>
                       <br/>
                       <label for="report">Intervention Description</label>
                       <input required type="text" value="" class="form-control" name="report" id="descrFrm">
                       <button type="submit" class="btn btn-rblue" id="btnForm" style="background-color:#EB2C55;"><i class="fa fa-check"></i>SUBMIT</button>
                       <input name="author" id="current_author"><%= current_user.id %>
                    </div>
                    </div>

                    <!-- DIV TEST FETCHING DATA RUBY -->
                    <p id="testdiv"><%= params[:customer_id] %></p>
                    
                    
                      
                    

                    

            </div>

</div><!--End main form coantainer-->

<a href="#" id="toTop">
  <span class="arrow-top">▲</span>
</a>

